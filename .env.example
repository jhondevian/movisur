DATABASE_URL="postgresql://postgres:postgres@localhost:5432/movisur"
AUTH_SECRET="cambia-este-secreto-por-una-cadena-larga-y-segura"
NEXT_PUBLIC_SITE_URL="https://tudominio.com"

# Telegram import. You can store the bot token from the admin panel,
# but TELEGRAM_BOT_TOKEN is recommended for production deployments.
# NEXT_PUBLIC_SITE_URL must be your HTTPS production domain before registering
# the webhook, for example https://movisur.com.
TELEGRAM_BOT_TOKEN=""
TELEGRAM_WEBHOOK_SECRET="cambia-este-secreto-telegram"
# Use your self-hosted Telegram Bot API for large files.
TELEGRAM_API_BASE_URL="http://127.0.0.1:8081"

# Optional: prefer this for production instead of storing the key in the DB.
IFREEICLOUD_API_KEY=""

# Use this when your PostgreSQL provider requires SSL.
# PGSSLMODE="require"
